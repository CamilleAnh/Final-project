<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:configChanges="locale"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.QuizzMaster"
        tools:targetApi="31">

        <activity
            android:name=".ResultActivity"
            android:exported="false" />
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name" /> <!-- AndroidManifest.xml -->
        <activity
            android:name=".LoginActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>

        </activity>
        <activity
            android:name=".AddQuestionActivity"
            android:parentActivityName=".MainActivity">

            <!-- Other activity configurations -->

        </activity>
        <activity
            android:name=".QuizQuestionsActivity"
            android:exported="true">

        </activity>
        <activity
            android:name=".DetailQuestionSetActivity"
            android:exported="true">

        </activity>
        <activity
            android:name=".AddQuestions"
            android:parentActivityName=".MainActivity">

            <!-- Other activity configurations -->

        </activity>
        <activity
            android:name=".EditQuestionSet"
            android:parentActivityName=".MainActivity">

            <!-- Other activity configurations -->

        </activity>
        <activity
            android:name=".Profile"
            android:parentActivityName=".MainActivity">

            <!-- Other activity configurations -->

        </activity>

        <activity
            android:name=".AdminActivity"
            android:exported="true" >

        </activity>
        <activity
            android:name=".ForgotPasswordActivity"
            android:exported="true" />
        <activity
            android:name=".RegisterActivity"
            android:exported="true" />
    </application>

</manifest>